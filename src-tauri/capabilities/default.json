{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main window, the tray flyout and the shortcut overlay",
  "windows": ["main", "flyout", "osd"],
  "permissions": ["core:default", "core:window:allow-hide"]
}
