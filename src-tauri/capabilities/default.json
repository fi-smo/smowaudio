{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main window and the tray flyout",
  "windows": ["main", "flyout"],
  "permissions": ["core:default", "core:window:allow-hide"]
}
