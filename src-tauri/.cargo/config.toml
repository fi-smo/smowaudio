# AVX2-era CPUs (x86-64-v3): fast on the Ryzen 9 5950X, and the same exe runs on the GitHub
# build machines and any PC from the last ~10 years. `target-cpu=native` would tie the exe to
# whatever CPU built it.
[build]
rustflags = ["-C", "target-cpu=x86-64-v3"]
